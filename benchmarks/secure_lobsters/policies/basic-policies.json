{
	"policies": [
		{
			"table": "invitations",
			"predicate": "WHERE invitations.user_id = UserContext.id",
			"description": "users are allowed to see their invitations"
		},
		{
			"table": "saved_stories",
			"predicate": "WHERE saved_stories.user_id = UserContext.id",
			"description": "users are allowed to see their saved stories"
		},
		{
			"table": "messages",
			"predicate": "WHERE messages.author_user_id = UserContext.id OR messages.recipient_user_id = UserContext.id",
			"description": "users are allowed to see their saved stories"
		},
	]
}
