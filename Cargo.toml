[package]
name = "distributary"
version = "0.1.0"
authors = ["Jon Gjengset <jon@thesquareplanet.com>"]

[workspace]
members = ["benchmarks", "consensus", "channel", "core", "mir"]

[features]
default = ["carry_local"]
profiling = ["timekeeper/default"]
binaries = ["default"]
generate_mysql_tests = ["default"]
carry_local = []

[dependencies]
clap = "2.25.0"
failure = "0.1.1"
fnv = "1.0.5"
futures = "0.1.16"
hostname = "0.1.3"
hyper = "0.11.6"
mio = "0.6.9"
mio-pool = "0.5.4"
mysql = "12.2.0"
rand = "0.4.0"
serde_derive = "1.0.8"
serde_json = "1.0.2"
slog = "2.0.6"
#slog = { version = "2.0.6", features = ["max_level_trace", "release_max_level_trace"] }
slog-term = "2.0.1"
tarpc = "0.9.0"
tarpc-plugins = "0.2.0"
tokio-core = "0.1.8"
slab = "0.4"
bincode = "1.0.0"

vec_map = { version = "0.8.0", features = ["eders"] }
timer_heap = "0.3.0"
serde = { version = "1.0.8", features = ["rc"] }

# git deps
petgraph = { version = "0.4.11", features = ["serde-1"] }
memcached-rs = { git = "https://github.com/jonhoo/memcached-rs.git", branch = "expose-multi" }
nom_sql = { git = "https://github.com/ms705/nom-sql.git", rev = "41a2c94c6b3f5947588610c3766d8eaf7e11ab6d" }

# local deps
channel = { path = "channel" }
consensus = { path = "consensus" }
core = { path = "core" }
dataflow = { path = "dataflow" }
mir = { path = "mir" }

# Not needed directly, but we want to force the version used by our
# dependencies to have default features. If this isn't here, then the features
# used for `num` will vary between subcrates, triggering unnecessary
# recompiles.
num = "0.1"

[dependencies.timekeeper]
version = "0.3.0"
default-features = false

[dev-dependencies]
backtrace = { version = "0.3.2", features = ["serialize-serde"] }
toml = "0.4.1"
diff = "0.1.10"
glob = "0.2.11"

[profile.release]
debug=true

[lib]
name = "distributary"
path = "src/lib.rs"

[[example]]
name = "basic-recipe"

[[bin]]
name = "souplet"
path = "src/bin/souplet.rs"
