{
	"policies": [
		{
			"table": "messages",
			"predicate": "SELECT * FROM messages, UserContext WHERE messages.author_user_id = UserContext.id",
			"description": "users can only view private messages which they either authored or received"
		},
		{
			"table": "messages",
			"predicate": "SELECT * FROM messages, UserContext WHERE messages.recipient_user_id = UserContext.id",
			"description": "users can only view private messages which they either authored or received"
		}
	]
}
