{
	"policies": [
		{
			"table": "messages",
			"predicate": "SELECT * FROM messages, UserContext WHERE UserContext.id = messages.author_user_id",
			"description": "users can only view private messages which they either authored or received"
		}
	]
}
