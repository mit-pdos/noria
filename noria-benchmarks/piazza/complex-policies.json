{
    "groups": [
        {
            "name": "ta",
            "membership": "SELECT r_uid AS uid, r_cid AS gid FROM Role WHERE r_role = 1 AND r_uid = ?",
            "policies": [
                {
                    "table": "Post",
                    "predicate": "SELECT Post.* FROM Post, GroupContext WHERE Post.p_private = 1 and GroupContext.id = p_cid",
                    "description": "tas are allowed to see private posts from classes they ta"
                }
            ]
        },
        {
            "name": "student",
            "membership": "SELECT r_uid AS uid, r_cid AS gid FROM Role WHERE r_role = 0 AND r_uid = ?",
            "policies": [
                {
                    "table": "Post",
                    "predicate": "SELECT Post.* FROM Post, GroupContext WHERE Post.p_private = 0 and GroupContext.id = p_cid",
                    "description": "students are allowed to see public posts from classes they are enrolled in"
                }
            ]
        }
    ],
    "policies": [
        {
            "table": "Post",
            "predicate": "SELECT Post.* FROM Post, UserContext WHERE Post.p_private = 1 AND UserContext.id = Post.p_author",
            "description": "users are allowed to see their private posts they authored"
        }
    ]
}
